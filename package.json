{
    "name": "timer-plugin",
    "version": "1.0.0",
    "description": "Plugin to count time spend on specified list of tasks",
    "main": "main/main.js",
    "author": "SonOfStorm",
    "license": "MIT",
    "scripts": {
        "start-dev": "concurrently \"cd ./renderer && BROWSER=none yarn start\" \"wait-on http://localhost:8080 && cd ./main && yarn a \""
    },
    "devDependencies": {
        "concurrently": "^5.2.0",
        "eslint": "^6.8.0",
        "eslint-config-prettier": "^6.11.0",
        "eslint-plugin-prettier": "^3.1.3",
        "prettier": "2.0.5",
        "wait-on": "^4.0.2"
    }
}
